import SwiftUI

@main
struct PRTrackerApp: App {
    var body: some Scene {
        WindowGroup {
            Text("PR Tracker — under construction")
                .frame(minWidth: 960, minHeight: 600)
        }
    }
}
